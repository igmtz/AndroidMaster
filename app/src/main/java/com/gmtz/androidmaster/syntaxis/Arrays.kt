package com.gmtz.androidmaster.syntaxis

fun main() {
    val weekDays = arrayOf("Lunes", "Martes", "Miércoles", "Jueves", "Viernes", "Sábado", "Domingo")

    for (positions in weekDays) {
        println(positions)
    }
}